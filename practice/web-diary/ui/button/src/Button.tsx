import styled                                      from '@emotion/styled'

import { baseStyles, greyColorStyles, sizeStyles } from './Button.style'

export const Button = styled.div(baseStyles, sizeStyles, greyColorStyles)
